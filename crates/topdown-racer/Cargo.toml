[package]
name = "topdown-racer"
version = "0.1.0"
edition = "2021"

[dependencies]
bevy = { version = "0.14", features = ["wav"] }
glam = "0.27"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
topdown-racer-core = { path = "../core" }
